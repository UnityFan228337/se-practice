# week-03/submission.yml — your declaration for this lab.
#
# Facts only. Every explanation goes in lab-report.md; nothing here repeats prose.
# Fill every value. Check it before you push:
#
#     python tests/validate_submission.py
#
# The validator checks shape, never quality. A green run is the floor, not the grade.
# If you cannot run Python, fill the file by hand against the comments — a malformed
# file costs you nothing if the content is there.

schema: 1
week: "03"

student:
  name: Мальцев Ярослав
  student_id: 25B030214         # as in KBTU records, e.g. 24B031016
  github:   UnityFan228337           # your GitHub username, the one that owns the repo

assistant:
  tool:  Gemini              # e.g. ChatGPT, Claude, Gemini, Copilot
  model:  Gemini 3.6 Flash     # the exact model name with its version. "ChatGPT" is not a model name.

counts:
  user_stories:   7         # how many stories you kept after your review (the task asks for 6-8)
  acceptance_criteria_sets: 3   # how many stories you wrote Given/When/Then criteria for

checker:
  # Numbers from YOUR last run of: python tests/check_requirements.py
  # They must add up to the number of checks it runs. Report them as they came out.
  # A FAIL you report and explain in lab-report.md costs you nothing. A hidden one costs the criterion.
  pass: 21
  fail: 2
  error: 0
  commit:     91ac7b4         # the commit you ran the checker at: git rev-parse --short HEAD
                       # (normally the commit just before you commit this file — that is fine)

assumptions:
  overlap_touching_bookings: "allowed"
  exactly_two_hours: "allowed"

traceability:
  use_cases_not_covered: []
  stories_not_traced: []

review_findings:
  - "US-07 was added during story review because UC-06 Send confirmation had no explicit user story generated initially."
  - "The AI originally connected Administrator directly to UC-06 Send confirmation, which was corrected to an <<include>> relationship from UC-02 and UC-03."
  - "AC-06 and AC-07 were explicitly expanded to cover mandatory business rules R1 (future booking) and R2 (max two hours) left out by generic happy path criteria."

honesty:
  can_explain_everything_submitted: yes
  ai_usage_disclosed: yes